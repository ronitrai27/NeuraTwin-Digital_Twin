import ProfileCompletion from "@/components/Profile-complition";
import React from "react";

const page = () => {
  return (
    <div>
      <ProfileCompletion />
    </div>
  );
};

export default page;
